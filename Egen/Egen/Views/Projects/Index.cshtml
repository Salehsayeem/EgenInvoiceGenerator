@model IEnumerable<Egen.Models.Projects>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int a = 1;
}

<br />
<h2 class="text-center">List of Projects</h2>

<a href="@Url.Action("Create")" class="btn btn-lg btn-success float-right">Create</a>
<br />
<br />

<table class="table table-responsive-md table-primary table-hover table-striped" style:width="100%">
    <thead class="black white-text">
        <tr>
            <th>#</th>
            <th>
                Project Name
            </th>
            <th>
                Project Code
            </th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tbody>
            <tr>
                <td>@(a++)</td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProjectName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProjectCode)
                </td>
                <td>
                    <a class="btn btn-sm btn-teal" href="@Url.Action("Edit",new {id = item.ProjectId})">
                        <i class="fa fa-pencil-square-o" title="Edit"></i>
                    </a>
                    <a class="btn btn-sm btn-danger" href="@Url.Action("Delete",new {id = item.ProjectId})" onclick="return confirm('Are sure wants to delete? ');">
                        <i class="fa fa-trash-o" title="Delete"></i>
                    </a>

                </td>
            </tr>
        </tbody>
    }

</table>

@section scripts{


}